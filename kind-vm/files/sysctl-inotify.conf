# kind imbrique des kubelets ⇒ beaucoup de watchers inotify. Les défauts Linux
# (128 instances) font timeouter kubeadm "wait-control-plane". On relève.
fs.inotify.max_user_instances = 8192
fs.inotify.max_user_watches = 1048576
