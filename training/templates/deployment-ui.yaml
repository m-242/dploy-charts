apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "training.fullname" . }}-ui
  labels:
    {{- include "training.labels" . | nindent 4 }}
    {{- include "training.ui.selectorLabels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "training.ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "training.ui.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "training.serviceAccountName" . }}
      automountServiceAccountToken: true
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: ui
          image: "{{ .Values.ui.image.repository }}:{{ .Values.ui.image.tag }}"
          imagePullPolicy: {{ .Values.ui.image.pullPolicy }}
          env:
            - name: PORT
              value: "8080"
            - name: SCENARIO_PATH
              value: "/scenarios"
            - name: SHELL_POD_NAME
              value: {{ include "training.shell.podName" . | quote }}
            - name: NAMESPACE
              value: {{ .Release.Namespace | quote }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          {{- with .Values.ui.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.ui.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: scenario
              mountPath: /scenarios
              readOnly: true
      volumes:
        - name: scenario
          configMap:
            name: {{ include "training.fullname" . }}-scenario
